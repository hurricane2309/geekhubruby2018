<%= link_to 'New supplier', new_supplier_path(@supplier) %>
<%= link_to 'All suppliers', suppliers_path %>
<p>
  <strong>Name:</strong>
  <%= @product.name %>
</p>

<p>
  <strong>Image:</strong>
  <%= image_tag @product.image.url, class: 'img-show' if @product.image? %>
</p>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
