<%= link_to 'New vendor', new_vendor_path(@vendor) %>
<%= link_to 'All vendors', vendors_path %>
<p>
  <strong>Name:</strong>
  <%= @supplier.name %>
</p>

<p>
  <strong>Image:</strong>
  <%= image_tag @supplier.image.url, class: 'img-show' if @supplier.image? %>
</p>

<%= link_to 'Edit', edit_supplier_path(@supplier) %> |
<%= link_to 'Back', suppliers_path %>
