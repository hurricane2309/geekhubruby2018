<p>
  <strong>Name:</strong>
  <%= @vendor.name %>
</p>

<p>
  <strong>Image:</strong>
  <%= image_tag @vendor.image.url, class: 'img-show' if @vendor.image? %>
</p>

<%= link_to 'Edit', edit_vendor_path(@vendor) %> |
<%= link_to 'Back', vendors_path %>
